#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int a, b;
    cout << "\nEnter side of a cube: ";
    cin >> a;
    b = pow(a, 3);
    cout << "\nVolume of cube = " << b;
}
